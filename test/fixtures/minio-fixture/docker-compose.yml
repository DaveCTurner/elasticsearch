version: '3'
services:
  minio-fixture:
    build:
      context: .
      args:
        bucket: "bucket"
        accessKey: "s3_test_access_key"
        secretKey: "s3_test_secret_key"
      dockerfile: Dockerfile
    ports:
      - "9000"
    command: ["server", "/minio/data1", "/minio/data2", "/minio/data3", "/minio/data4"]
  minio-fixture-other:
    build:
      context: .
      args:
        bucket: "bucket"
        accessKey: "s3_test_access_key"
        secretKey: "s3_test_secret_key"
      dockerfile: Dockerfile
    ports:
      - "9000"
    command: ["server", "/minio/data1", "/minio/data2", "/minio/data3", "/minio/data4"]
  minio-fixture-repository-test-kit:
    build:
      context: .
      args:
        bucket: "bucket"
        accessKey: "s3_test_access_key"
        secretKey: "s3_test_secret_key"
      dockerfile: Dockerfile
    ports:
      - "9000"
    command: ["server", "/minio/data1", "/minio/data2", "/minio/data3", "/minio/data4"]
